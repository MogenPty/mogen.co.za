---
import { Image } from "@astrojs/image/components";
import config from "@config/config.json";
import { plainify } from "@lib/utils/textConverter";

const { folder, summaryLength, post, index } = Astro.props;
---

<div class="post">
  {
    post.data.image && (
      <Image
        class="rounded-lg"
        src={post.data.image}
        alt={post.data.title}
        width={index === 0 ? 925 : 445}
        height={index === 0 ? 475 : 230}
      />
    )
  }
  <h2 class="h3 mb-2 mt-4">
    <a href={`/${folder}/${post.slug}`} class="block hover:text-primary">
      {post.data.title}
    </a>
  </h2>
  <p set:html={plainify(post.body.slice(0, summaryLength))} />
  <a class="btn btn-primary mt-4" href={`/${folder}/${post.slug}`} rel="">
    Read More
  </a>
</div>
